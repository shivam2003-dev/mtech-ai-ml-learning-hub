# Mathematical notation

We use $p,q$ for propositions, $C$ for a clause, $F$ for a formula, $M$ for a
model, and $\Gamma\models\varphi$ for semantic entailment. A bar over a
literal denotes negation. A proof is a finite sequence of justified steps; a
model is an assignment that makes every formula in a set true.
