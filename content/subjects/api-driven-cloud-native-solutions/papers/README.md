# Paper reading path

Use the citation records in `references.md` as the first path. For each paper,
write: problem, system boundary, assumptions, method, evidence, limitation,
and one question for an ML API design. Do not treat a systems result measured
on one workload as a universal production recommendation.
