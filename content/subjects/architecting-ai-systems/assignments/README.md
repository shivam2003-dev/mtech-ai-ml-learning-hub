# Assignments

1. Compare a modular monolith and an event-driven architecture for the running
   document-triage case. Include owners, contracts, latency, cost, failures,
   rollback, and governance.
2. Build a six-stage ADLC evidence manifest linking data, code, model, policy,
   evaluation, approval, and deployment identifiers.
3. Extend the chapter validator with bounded retry, idempotency, and tool
   approval checks. Write negative tests first.
4. Measure p50, p95, and p99 under a declared workload and explain queueing
   assumptions and confidence limits.
5. Threat-model an MCP/A2A-style gateway that can search but cannot delete;
   propose least privilege, audit, sandbox, and human approval controls.

Submit code, commands, raw results, assumptions, licences, and limitations.
