# Papers

This compatibility directory intentionally contains no fabricated paper pages. Approved paper notes will be generated only after the shared content pipeline chooses the canonical `papers` or `research-papers` route.

