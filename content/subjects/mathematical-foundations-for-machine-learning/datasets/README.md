# Datasets

No external dataset is required for the reference chapter. Its numerical examples use explicit, non-personal synthetic arrays. Future datasets must include provider, version, licence, canonical URL, intended fields, and privacy review.

